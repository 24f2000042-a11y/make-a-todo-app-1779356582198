document.addEventListener('DOMContentLoaded',function(){var nav=document.querySelector('.navbar');window.addEventListener('scroll',function(){nav.classList.add('scrolled')});var menuToggle=document.querySelector('.menu-toggle');var navLinks=document.querySelector('.nav-links');menuToggle.addEventListener('click',function(){navLinks.classList.toggle('active')});var todoForm=document.getElementById('todoForm');var taskInput=document.getElementById('taskInput');var dueDate=document.getElementById('dueDate');var taskList=document.getElementById('taskList');var emptyState=document.getElementById('emptyState');todoForm.addEventListener('submit',function(e){e.preventDefault();var taskText=taskInput.value;var date=dueDate.value;if(taskText&&date){var taskItem=document.createElement('div');taskItem.className='task-item';taskItem.innerHTML='<div class="task-header"><span class="task-text">'+taskText+'</span><div class="task-actions"><button onclick="this.parentElement.parentElement.remove()"><i class="fas fa-trash"></i></button></div></div><div class="task-date">'+date+'</div>';taskList.appendChild(taskItem);taskInput.value='';dueDate.value='';emptyState.style.display='none';}todoForm.reset()});if(!taskList.children.length){emptyState.style.display='block'}AOS.init({duration:800,once:true})})